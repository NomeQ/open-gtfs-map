extends layout

block content
  h1= title
  
  form(method='GET' action='map')
    div.form-group
      label(for='gtfs_url') GTFS URL:
      input#gtfs_url.form-control(type='text', name='gtfs_url')
    button.btn(type='submit') Generate Map